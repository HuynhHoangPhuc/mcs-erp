root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["tmp", "web", "node_modules", "migrations", "proto"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true

// Re-export from @mcs-erp/api-client package (centralized fetch wrapper).
// This file preserved for backward compatibility with existing imports.
export { apiFetch, ApiError, setAccessToken, getAccessToken } from "@mcs-erp/api-client";
